import { notImplemented } from '@/app/api/marketplace/_lib/response';

// TODO(step 2): implement. Reads hit getMarketplaceDb(), never the site's DB.
export async function GET() {
  return notImplemented('/api/marketplace/orders/[id]');
}
